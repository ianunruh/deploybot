version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - copyloopvar
    - errorlint
    - misspell
    - unconvert
    - usetesting
  exclusions:
    generated: lax

formatters:
  enable:
    - gofmt
    - goimports
