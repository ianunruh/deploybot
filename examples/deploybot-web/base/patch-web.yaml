apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploybot-web
spec:
  template:
    spec:
      containers:
        - name: web
          env:
            - name: DEPLOYBOT_API_URL
              value: http://deploybot:8080
